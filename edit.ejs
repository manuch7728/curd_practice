<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a class="text-zinc-500" href="/read">View Users</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-3">Update User</h1>

        <form action="/edit/<%= user._id %>" method="post" class="space-y-4">
            <input class="px-5 py-3 border-2 border-zinc-800 outline-none bg-transparent w-full"
                   type="text" name="name" placeholder="name" value="<%= user.name %>" required>
            <input class="px-5 py-3 border-2 border-zinc-800 outline-none bg-transparent w-full"
                   type="email" name="email" placeholder="email" value="<%= user.email %>" required>
            <input class="px-5 py-3 border-2 border-zinc-800 outline-none bg-transparent w-full"
                   type="text" name="image" placeholder="image url" value="<%= user.image %>">
            <input class="px-5 py-3 bg-blue-500 rounded-lg cursor-pointer"
                   type="submit" value="Update user">
        </form>
    </div>
</body>
</html>
